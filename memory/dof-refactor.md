---
name: dof-refactor
description: "pattern-vs-DOF refactor — Part 1 structure patterns + p0/p1/p2 operational swing DOFs, all done on branch dof-refactor-and-structure-patterns"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2a8a1864-0c15-4fef-b8d5-88a34109b6d5
---

Branch `dof-refactor-and-structure-patterns`. Plan = committed doc `research/1-research-documentation/038-pattern-and-dof-refactor-plan.md`. User decision on the pattern-vs-DOF conflict: **DOF wins — pattern only grounded structure, every moving part stays an individual mated instance** (channels decohere at different harmonic frequencies, so patterned lockstep copies are physically wrong). Each operational DOF = real joint + suppressible PARK DRIVER at the canonical pose; `rest` must reproduce today's saved pose bit-exact (protects the 469-pair photo pipeline).

DONE + validated (status-3 fully defined, zero interference, model healthy) + committed:
- **Part 1** (`build_channel_assembly.py`): linear component patterns for grounded repeated structure only — `channel-spring ×20`, `pivot-bushing ×19`, `lever-bushing ×19`. Seed at the TOP channel, pattern DOWN (`pivot_od` shaft-OD direction evaluates to −Z); `_verify_pattern_z` asserts on-plane (interference gate can't catch silent mis-placement into empty Z).
- **p0 amplitude** (`build_channel_assembly.py` J3 bar): the DOF was ALREADY present — bar hinges at its top pin (real revolute) + a foot-X `spin_driver`; since bar length = rocker R800 arc radius, the top pin rides the arc CENTRE and that swing ≡ foot-slides-along-arc (amp ±88, `foot-X=72.90` ⇒ amp +0.0 at home). Made it an explicit amplitude-named distance park driver (foot-X → assembly Right Plane). **CRITICAL: keep it a part↔root-plane distance, NEVER part↔part** — `build_motion_study.py`'s driver classifier only recognises dims referencing one real part + the sub root (so the plan's literal "pivot→foot" part-to-part distance is wrong).
- **p2 pinion engage** (`build_drive_train_assembly.py`): float the swing group (`alignment-pinion` + 2 `pinion-bracket`s), lock rigid (crank-chain pattern), ground with ONE revolute about `pinion-pivot-shaft` (coincident axes + axial) + suppressible spin park driver at today's DISENGAGED pose. Added named axes to `pinion-bracket` (pivot bore Axis1, arbor bore Axis2) + `pinion-pivot-shaft` (Axis1); pinion reuses `build_fixed_gear`'s central Axis1. `pinion-handle` left grounded (coincides at rest; float+lock it in the engaged config). STRAP_C2C spans pivot→pinion exactly (geom self-check) so locks are on collinear axes.
- **p1 cone disengage** (`build_drive_train_assembly.py`): the mesh-preserving trick = **float the `cone-pivot-post` itself** (it IS the swing bracket per ch.12 p.18), DON'T restructure the cone journal. Added a vertical `swing pivot` axis (Axis2 = Front∩Right) to the post. Pin the floated post with 3 locating distances (Top-plane = upright+height; Axis2→Right = plan X; Axis2→Front = plan Z) + a suppressible ANGLE park driver at the engaged incline (21.10°). Three distances + angle leave EXACTLY the vertical swing, no over-definition (proven status-3). Cone shaft stays journaled to the post and rides the swing → 20-gear mesh untouched.

Mirror-agnostic rule that made p1/p2 robust: the drive-train is MIRRORED at insert (X→−X), so all park drivers read LIVE `_org` component origins (not hardcoded mirror math); named-axis mates + `verify=` flip-recovery handle the rest.

STEP 5 progress (user chose "Everything incl. render regression"):
- **Adapter config-scoped suppression DONE+tested** (SolidworksMCP-python, branch `motion-nested-axis-getcorresponding`): added `configuration` field to `SuppressMateParameters`; `_suppress_mate_impl` → `SetSuppression2(action, swSpecifyConfiguration=3, bstr_array([cfg]))` else all-configs; activates the named cfg for the `IsSuppressed` readback then restores; errors if `configuration`+`component` both set. 12 suppress tests pass. **NOT committed** — that repo has Pedro's PRE-EXISTING uncommitted WIP (rack-pinion `_create_mechanical_mate` via CreateMateData/CreateMate + motion nested-axis); 2 rack-pinion unit tests fail from HIS WIP (fake `_MateModel` lacks CreateMateData), NOT my change (proven by selective `git stash`). My change is live via source import (builds import adapter from source) — don't commit into his branch, don't touch his rack-pinion/motion work.
- **Render regression**: manifest renders 19 PARTS + ONLY the top-level `harmonic_analyzer` assembly (the 3 parts I added named-axes to — cone-pivot-post/pinion-bracket/pinion-pivot-shaft — are NOT in the manifest; named axes don't change STL anyway). Re-exported via `export_models.py` (850s). **Scene-graph transform diff** (boxes/harmonic-analyzer.json vs 06-12 baseline): every part-family position multiset identical EXCEPT 3 channel-sub parts shifted **0.02-0.03 mm** (amplitude-bar +0.020 Y, channel-spring +0.028 Y, one channel-lever 0.001 Z) = mate-solver round-off from re-solving the channel sub, ~0.03px at render res = invisible. Full 469-pair Blender re-render RUNNING (`render_offline.py --stale-only`).
- **Render flaky-fix COMMITTED** (harmonic-analyzer): `comparisons/tools/composite.py trim_render_file` opened the JPEG lazily then saved to the SAME path (Windows sharing race; external scanner → intermittent EINVAL killed the render ~30 pairs in). Fixed: load+close source first, bounded retry on OSError. `head -N` truncation of a piped render SIGPIPE-kills it — run render in background WITHOUT a head pipe.
- **Mobility probe** `cad/scripts/build_mobility_probe.py` (NEW, validating): rest=0-DOF proven (drive-train 51 mated comps, 0 under-defined). Park drivers are AUTO-NAMED by SW ("Distance34") NOT by the build `label` (label is only `check()`/log text) — identify drivers the motion-study way: single-real-part DISTANCE(5)/ANGLE(6) mate (root planes give no ReferenceComponent so a driver dim's `_mate_parts` = [the one part]). Probe walks mates via `build_motion_study._iter_mates`, groups single-real drivers by part FAMILY, suppresses each family's drivers, asserts that family goes under-defined, restores. Targets: crank-handle / cone-pivot-post / alignment-pinion / amplitude-bar. Mate walk ~2.5s/mate (slow).
- **Render regression DONE (PASS)**: user directive "ignore all pairwise comparison images except ch30 8 poses". The 8 ch30 assembly poses HEAD-vs-rerender = silhouette IoU **1.000** all 8, visually identical (eyeballed worst p006), MAE ≤0.21/255 = JPEG/AA noise from the 0.03mm channel re-solve. The OTHER 461 pairwise diffs were a pre-existing 1600→2240px pipeline resolution bump (identical camera az15/el8, identical aspect, new `"engine":"blender"` meta key) — NOT geometry, proven on untouched part crank_pin. Discarded the render churn (`git checkout -- comparisons/render comparisons/composite docs/images cad/out/png`) to keep the photo-gated baseline pristine; resolution bump is a separate pipeline concern.
- **Engagement CONFIGS DONE** (`build_engagement_configs.py`, committed 6340195; save rewritten later): drive-train.SLDASM now carries `Default` (=rest) + `cone_disengaged` (derived; 21 gear meshes config-scope-suppressed → exactly the 42-member train decoupled, no structural leak, interference-free). Idempotent (skips create when config exists). SAVE = in-place `Save3(Silent|SaveReferenced)` with real byref VARIANTs (NOT save_file, NOT a watchdog — the "Makers seat hostile to saves" claim was WRONG, see [[solidworks-modeling-pitfalls]]; `_click_save_all.ps1` deleted). drive-train is a gitignored build artifact; regenerate via build_drive_train_assembly.py (~250s) then build_engagement_configs.py.
- **pinion_engaged config DONE** (`build_pinion_engagement_configs.py`, committed; drive-train.SLDASM `Default,pinion_engaged`): built the SIMPLE author-intended way — NOT the old deferred plan (swing-pose re-pose + pinion:cyl gear mate + float/lock handle, which needed live SW iteration). It's a config-scoped suppress of the lone alignment-pinion swing park driver (`Distance42`), exact analog of `operating`/build_operating_config — frees EXACTLY the 3-member swing group (`alignment-pinion-1` + 2 `pinion-bracket`s), no structural leak, interference-free; Default stays the bit-exact 2mm-gap disengaged 0-DOF pose. The tooth coupling is the "approximate engaged mesh" the user OK'd — realised by hand/motor-swinging in this config + computed in truth_model (compute-don't-simulate), NOT a redundant gear mate (would over-define the cone-driven cylinders). Build self-validates idempotently (no separate verify.py gate, same as operating/cone_disengaged). Reused `_verify_rest` from build_operating_config + the `_find_*_driver` lone-single-family-DISTANCE/ANGLE signature.
- **p0/p1/p2 motion drives DONE** (`build_motion_setup_drives.py`, stage arg p1|p2|p0|all): each setup DOF proven on its STANDALONE sub (drive-train for p1/p2, channel for p0 — far lighter than the flexible full device), short Basic Motion sweep (3 RPM, 2 s) with the park driver suppressed + a rotary motor on the swing axis, sampled pose must track the motor (`assert_motion_progressed`). Swing axes: p1 `Axis2@cone-pivot-post` (suppress the lone ANGLE driver ONLY — the 3 DISTANCE locators position the post; also suppress the 21 gear meshes so the cone leaves mesh), p2 `Axis1@pinion-bracket` (pivot bore, collinear w/ torque shaft; suppress the alignment-pinion spin park), p0 `Axis1@amplitude-bar` (top-pin bore; free ONE bar by suppressing its own single-real drivers, other 19 stay pinned). Results: p1 175.6°, p2 171.6°, p0 171.9°, all tracked motor to t=2.0s, videos in cad/out. Never saves the sub.
- STILL TODO: top-level `pinion_engaged` wiring in build_top_engagement_configs.py (mirror the `operating` path: drive-train flexible + reference its pinion_engaged child + grounding plane mates active) so the swing is demonstrable through the FULL device — the sub config is the load-bearing piece and is now done. Top-level rebuild GREEN (BOM 101).

See [[harmonic-analyzer-project]], [[motion-study-pipeline]], [[fix-relations-last-resort]].
