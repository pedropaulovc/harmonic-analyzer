---
name: fix-migration-status
description: "Fix-relation retirement migration — adapter PRs #55/#56 MERGED, B1-B3 done, validation green, bead-chain rework done; B4 final gates remaining"
metadata: 
  node_type: memory
  type: project
  originSessionId: ba03bcc4-d81e-4e71-bbc7-7926c9a87d29
---

Status of the fix-relation retirement in [[harmonic-analyzer-project]] (directive: [[fix-relations-last-resort]]), as of 2026-06-12.

**Adapter (SolidworksMCP-python, branch `personal`) — DONE.** PR #55 (point refs + 6 relations) and PR #56 (point-pair dims + diagnostics) both MERGED. Surface: point refs `"Circle_1.center"`/`"Line_2.start"`/`"Line_2.end"`/`"origin"`; relations incl. `merge`(42), `horizontal_points`(25), `vertical_points`(26); dim types `horizontal_distance`/`vertical_distance` (AddHorizontalDimension2/AddVerticalDimension2) and aligned `distance` (**AddDimension2** — `swSmartDimensionDirection_e` has no aligned member, `Extension.AddDimension` returns None for point pairs); `get_over_defining_relations()` (GetRelations(2), mapped to relation names). Live smoke: `scripts/demo_point_anchoring.py`. Gotchas fixed in #56: angular dims never got values (hoisted value-set block); `read_segment_endpoints` must call-then-property (`flag_methods` on a line dispatch flips `GetStartPoint` to method resolution); lines created at coincident coordinates auto-merge at creation so an explicit `merge` then FAILS — author chains with offset joints if merge relations are wanted.

**Repo B (harmonic-analyzer):** checklist at `cad/FIX_MIGRATION.md`. B0+B1 committed: `define_circle` now semantic (coincident at origin / `*_points`+1 dim on-axis / 2 distance dims general, then DRIVING diameter); `ensure_fully_defined` assert-only with `get_over_defining_relations` in over-defined errors; legacy escalation behind `allow_fix_escalation=True` at exactly 2 whitelist-candidate sites (equation curves: `_common.add_spring_end_hooks`, `_gear.cut_tooth_gap` — B3 tries semantic schemes there). New helpers `anchor_point_to_origin`, `dimension_between`. Builds run with `C:\src\SolidworksMCP-python\.venv\Scripts\python.exe` (harmonic repo has no venv).

**B2 DONE** (25 PASS / 36 FAIL, log `.b2_results.log`); **B3 source migration COMPLETE 2026-06-12** — all 36 FAILs + every remaining `fix_entities` site migrated (helpers `define_rectilinear_chain` / `define_polygon_chain` in _common; revolve profiles are CLOSED chains whose on-axis closing segment absorbs the merged centerline). Spring hooks (`add_spring_end_hooks`) reverted from equation curves to real line + 270° arc, 7-constraint scheme (vertical lead anchored+dimensioned, tangent merged junction, radial, open end `vertical_points` to centre). Final whitelist = 3 gear-gap equation-curve sites (`_gear.py:111`, `build_cone_gear.py:~535`, `build_transgear_removable.py:~271`), each `allow_fix_escalation=True` + justification; escalation branch in `ensure_fully_defined` kept permanently behind that flag. Off-axis revolve profiles (magnifying-bracket collar): centerline shares NO vertex with the profile → needs own horizontal + length dim + origin anchor. Tangent-loop chains (drive-chain): per loop 11 DOF = 2 centre anchors + 3 radials + 4 junction tangents (merged tangent junctions). Arc redundancy trap: anchored centre + anchored endpoint forces radius — only ONE radial dim per dome pair.

**Validation:** batch 1 (22) ALL PASS; batch 2 (26) ALL PASS after the transgear seam-edge fix (see [[solidworks-modeling-pitfalls]]); batch 3 (10) 9 PASS — drive_chain's over-defined band was RETIRED instead of patched (user redirect 2026-06-12): the chain is now a SolidWorks **chain component pattern** of `chain-bead` spheres (Ø4.8, 63 at exact-closure pitch 6.3874) over `_chain.py`'s centreline loop, built in `build_output_assembly._insert_bead_chain` (path sketch authored post-mirror: x negated, arc ends swapped; scheme = 2 centre anchors + 3 radials + ALL 4 junction tangents, proven in diag_chain_loop.py). output.SLDASM rebuilt green: 122 comps, beads on-path 0.0000, zero interference. Chain-pattern COM facts in [[solidworks-modeling-pitfalls]].

**Remaining — B4 final gates:** top-level assembly rebuild (output.SLDASM changed: BOM/renders stale, +62 bead rows), render comparisons (ch30 chain plates now show discrete beads), closing sweep (`grep -rn '"fix"' cad/scripts/` → only the 3 justified sites), update DIMENSIONS/README counts if needed, delete `cad/FIX_MIGRATION.md`, push.
