# Memory index

- [Zero late-binding task](zero-late-binding-task.md) — in-progress task #7 after the early-bound merge: rebind all 25 empirical late-binding fallback sites to their correct interface (drawing→IDrawingDoc, GetComponents→IAssemblyDoc, GetBodies2→IPartDoc, Select2→IEntity/ISketchSegment, ViewZoomToFit2 case fix, …), then flip `_fallback_subclass.__getattr__` to RAISE; on `pedro/` branch off personal, validate full cold build with ZERO fallback warnings
- [Submodule CI coverage gate](submodule-ci-coverage-gate.md) — the `CI` workflow's plain `test` job on SolidworksMCP-python is chronically red (unmeetable 90% coverage gate in SW-free CI) and non-required; judge PR health by `pedro-ci` matrix + ruff, don't chase it
- [SolidWorks center-rectangle determinism](solidworks-center-rectangle-determinism.md) — native `CreateCenterRectangle` only anchors its centre to the origin when the per-seat option `swSketchAddConstToRectEntity` (swconst id 584) is ON; it's READable but NOT settable via COM on R2026x, so `define_centered_rectangle` must add an explicit `midpoint(origin, diagonal)` anchor (idempotent) — main PR #283; this was the true cause of the "16-part under_defined" scare, a seat option drift not a code regression
- [Bench Opus run setup](bench-opus-run-setup.md) — run comparisons/bench for Opus: source geometry from a release into cad/out (touch stl/boxes newer), regenerate 270 renders, `--effort high`, run-until-quota driver + periodic push to feat/bench-opus-high (PR #253)

- [Fastener policy: US-customary UNC](fastener-policy-us-customary.md) — ALL threads/holes use ANSI inch UNC (1/4-20, #10-24, #5-40, 9/16-12), Hole Wizard features carry the designations; period BSW research file deliberately IGNORED; best-fit sizing when undefined
- [DimXpert block tolerance / title block](dimxpert-block-tolerance.md) — .XX ±.02in / .XXX ±.005in / ±1° / Ra 125 from title_block.yaml (own file; displays are mm renderings until #290); TOL_* custom props feed cad/templates/harmonic-analyzer.DRWDOT via $PRPSHEET, finalize_drawing requires them on the linked model; DimXpert decimals+angular prefs are GET-ONLY on R2026x (repro in diagnostics/); repo-owned harmonic-analyzer.PRTDOT is pinned as the seat default at every connect + folded into part cache keys — builds RAISE on drift
- [Hole Wizard COM recipe](hole-wizard-com-recipe.md) — `_holes.wizard_holes` probe-verified; blind InitializeHole poisons the session; early-bind definitions; never write HoleFit on a plain hole (ineffective and converts type 25 into bogus far-countersink type 26); compare initialized ThruHoleDiameter before diameter overrides; #4 normal is native #30 diameter 3.2639 mm while #8 normal still needs forcing; production override audit + isolated repro included
- [COM seat lock](com-seat-lock.md) — SolidWorks serialization is a runtime cross-process file lock (`_com_seat`/`filelock`, machine-global `%PROGRAMDATA%` path), NOT the old `task_dep` spine (removed 2026-07-11); every COM subprocess grabs the seat before driving COM; DAG now carries only real edges (export gated on verify, release on export+preflight+verify+check); new COM tasks MUST wrap `_com_seat`/`com=True`, enforced loud in `_common.run_build`
- [Drawing recipe COM pitfalls](drawing-recipe-com-pitfalls.md) — pin every drawing view's scale (auto-scale silently breaks coordinate picks), pick hole RIMS not centers, GetOutline pads a margin, chamfer moves the hole-table origin, "⤓ 0.000" = tapped thru, 178 mm part needs a 1:2 ASME B sheet
- [Codex drawing image review](codex-drawing-image-review.md) — no-context machinist review of drawing PNGs via `codex exec -m gpt-5.6-sol -c model_reasoning_effort=high -i sheet.png - < prompt.txt` from an EMPTY dir (stdin prompt, never inline); triage findings cheap-notes/layout/foundation; stop after ~2 rounds
- [Parallel SW instances investigation](parallel-sw-instances-investigation.md) — QUEUED (post-PR #193): probe whether 2+ independent SolidWorks instances can run parallel COM builds on one seat (ROT-bind a specific instance, no flip flakiness); acceptance = 3× full COLD build must pass; would relax the `dodo.py` COM seat-lock serialization (see [[com-seat-lock]])
- [Physical build intent](physical-build-intent.md) — model is destined for a REAL machined build (period brass+steel) once validated; manual mill+lathe primary for fidelity, PM-30MV **CNC** for repetitive parts (cams/spacers/gears); `tolerance-gdt-assessment.md` §11 + `tolerance-policy.md` revised to CAM/DXF **IN SCOPE** (CAM deferred until nominal model frozen)
- [Period-accurate fasteners ON HOLD](period-accurate-fasteners-on-hold.md) — Whitworth/BA/Sellers fasteners SUSPENDED until further notice (2026-07-11); CAD code must use US customary (inch) UNC/UNF fasteners instead; the two research keys carry a matching WARNING banner and are reference-only
- [Codex Windows sandbox broken](codex-windows-sandbox.md) — codex plugin `--write` (workspace-write) blocks ALL commands on this seat; bypass with direct `codex exec --sandbox danger-full-access`
- [Pose studio part-delta fitting](pose-studio-part-delta-fitting.md) — align pair → hand-fit parts in Blender → Export Part Deltas → map_deltas.py back to build-script dims, with confidence-tag gate + depth-ambiguity gotchas (PR #276)
- [Pose studio camera toggle](pose-studio-camera-toggle.md) — Blender Numpad-0 exit jump: stock `view3d.view_camera` restores its own pre-camera rotation (loc/dist survive, rotation reverts); fixed with a keymap toggle op (direct `view_perspective` assign) + a persistent watcher timer; verify via `pose_studio.py --shots` (`render.opengl`, not `screen.screenshot`); stale ENABLED `blender_camera_addon.py` in %APPDATA% was force-locking the camera — removed

- [Default-free DOF + park drivers](default-free-dof-park-drivers.md) — park machinery KILLED 2026-07-09 (Pedro: build is deterministic without full definition): no PARK_* mates, no `locked` mode/`build_lock.yaml`, no release closure proof. Freed operational DOF (drive-train 4, channel 3/channel, magnifier 3, paper-drive/summing/pen 1) get NO driver mate; `free_dof_key=` records each drive spec into the `.<stem>.dof.json` KINEMATIC DOF MANIFEST for transient replays (`author_dof_drives` → `DRIVE_<key>`, verify:kinematics + diagnostics); soundness proves the free set exactly (`assert_free_dof_necessity` + `allowed_stems` exact-set). TRAP: mate labels mentioning "PARK driver" stay verbatim (flip-ledger keyed)
- [No untested failure assumptions](no-untested-failure-assumptions.md) — don't design defensive fallbacks (locked-pose checks, forced-lock exports) around an UNVERIFIED assumption that SolidWorks will misbehave; enforce real checks on the as-built artifact, reproduce a failure empirically before mitigating it
- [Negative result needs a positive control](negative-result-positive-control.md) — never declare an API/feature dead from self-authored failures alone (3 interventions on CopyWithMates2): reproduce a form known to work (UI/VBA-via-RunMacro2-.swb/official example), bisect the delta one variable at a time, exhaust examples in ALL dialects + the web, and word verdicts as "dead under <tried>; untested: <list>"
- [Load-bearing claims need a repro](load-bearing-claims-need-repro.md) — a written "X doesn't work" claim that gates architecture must staple a re-runnable repro, else it calcifies into unfalsifiable folklore: the false "late binding is forced, always" runbook claim (early-bound `OpenDoc6` DOES work via `InvokeTypes`) forced `flag_methods` (~155ms/obj) everywhere and cost dozens of hours; origin = upstream PR #14 (DPerrault), Copilot-bot + human reviewed, NOT Codex; inherited-claim twin of [[no-untested-failure-assumptions]] + [[negative-result-positive-control]]
- [CopyWithMates2 free-DOF is by design](cwm-free-dof-by-design.md) — the CopyWithMates2 "wander" (#227/#231/#232/#233) is EXPECTED SW behavior, not a bug: an under-constrained copy's free DOF has no mate-defined value, so it re-solves off the seed's; `--pin` (fully-constrain) lands it on pose. Correct `NewEntityToMateTo` usage doesn't change it; the bool return is False even on success (incl. SW's own profile-center example). the Transform2-put-REVERTS-after-rebuild "symptom" is the documented solver-state attractor — SAME free-DOF root, NOT a bug, already characterized (`diag_cwm_attractor.py` #227) AND mitigated (put→transient-driver→delete in `build_channel_assembly`/`_cwm.py`); minimal case shows put HOLDS (scale can't summon the attractor). Only open item is perf: IDragOperator vs transient drivers on the real slice. Diagnostic `diag_cwm_min.py`
- [CopyWithMates2 NewEntityToMateTo + flip idiom](cwm-newentity-flip-idiom.md) — the correct way to re-value an external dim in a copy: Repeat=false + NewEntityToMateTo (`resolve_entity`) HONOURS FlipDimension; a MIXED Repeat array works so touch ONLY the dim slot. PR #236 (supersedes #228) dropped BOTH flip workarounds — the channel's always-positive ladder (now local PITCH/2 off each copy's OWN gap bushing) and the drive-train's `set_distance_flip` heal. Attractor is independent → channel put+driver STAYS. Both assemblies rebuilt green. Extends [[cwm-free-dof-by-design]]
- [Verify assumptions in live SW](verify-assumptions-live-sw.md) — treat old code comments as likely stale; before sizing/rejecting a CAD or mate fix, probe the live model (features, named axes, transforms, mirror flags) and try the candidate mate empirically — let results, not prose, set the design and scope
- [Belt/Chain feature — full pywin32 recipe](belt-chain-feature-com-binding.md) — creates fine (swFmBeltAndChain=119). For a correct EngageBelt coupling on TOOTHED wheels the pulley members must be DATUM AXES (`pulley_member_axes`, submodule #79): with FACE members the `MateBeltDim` bakes the picked faces' TIP diameters (28:52=0.538) and EVERY definition-level rewrite is inert despite green read-backs (PulleyDiameters+early-bound ModifyDefinition, ModifyMemberParameters, EngageBelt re-author, direct mate-dim writes — all measured live); axis members → typed diameters drive the mate, measured +0.5000 same-sense. Adapter verifies the mate's own D1/D2 post-create. Traps: getters read [] until `AccessSelections`; ModifyDefinition needs an EARLY-bound IFeature. FOUR wrong conclusions on this feature — verify behaviour, not read-backs — extends [[chain-component-pattern]]
- [Paper-drive kinematic probe](paper-drive-kinematic-probe.md) — `build_kinematic_probe.py` drives crank +30° and proves T24/knob-shaft/fine-pinion/platen all follow (wired into verify:kinematics as `paper-drive:crank-feed`); the crank→T24 tie is the BELT/CHAIN feature with AXIS pulley members + typed pitch diameters (round-5's gear mate rejected: right ratio, WRONG same-sense — an external mesh reverses; the post-create ModifyDefinition "enforcement" also rejected — definition green, mate unchanged); probe asserts ratio 0.500±0.03 AND signed-Z same-sense; knob shaft spins about global Y ⊥ sprockets' Z, so cluster compares are MAGNITUDE-only (`acos((trace−1)/2)`); temp angle drivers must start off the 0°/180° apex. NON_PART_SCRIPT, never saves
- [Chain component pattern (SHIPPED)](chain-component-pattern.md) — paper-drive roller chain is now a native connected-linkage chain component pattern (`pattern_components_chain` adapter method → `IFeatureManager.FeatureChainPattern`), build+verify:soundness green. Two gotchas that cost the day: (1) the PATH must be a single connected sketch SEGMENT (`EXTSKETCHSEGMENT`, e.g. a closed spline) — selecting the sketch FEATURE → CreateFeature silently null; (2) under pywin32 late binding `CreateDefinition`/`CreateFeature` returns null (works only in early-bound C#), so use the one-call `FeatureChainPattern`. Property getters lie pre-commit. UI macro-recording (`.swp`→`olevba`) cracked it. Never in-process `EnsureModule`/`CastTo` on the SW TLB — wedges the seat
- [Mate flip determinism](mate-flip-determinism.md) — flip side is deterministic per mate; winner (#185) = distance mate + SIGN-DERIVED flip seed (0 flips, 0 planes, ~2.5× faster than signed-offset planes, which were closed #176/#179); learn `_FLIP_INVERT` from the build's own `flip-seed MISS` warns; orientation-aware sigs (`_orient_suffix`) fix mirror/rotation-twin collisions — the fake "non-determinism"
- [DXF import on Makers seat](dxf-import-makers-seat.md) — SW 3DEXPERIENCE Makers import ignores SetPosition AND SetSheetScale for flat modelspace (author the DXF at final plate-mm, import scale=1/pos=0); traced line-art has no cut regions until each stroke is buffered into a closed ribbon (shapely) — how PR #143 got a real ~210 mm³ engraving
- [Direct-script build uses stale parts](direct-script-build-stale-parts.md) — running an assembly `build_*.py` directly uses on-disk parts (won't rebuild stale ones); `Failed to select mate entity (AXIS/PLANE at '<Datum>@<part>')` = stale part missing that named datum, not a mate bug — rebuild the part; isolate MY-change-vs-env by `git checkout <old> -- <file>` in the submodule and re-running
- [Replacing mechanisms → preserve invariants](replacing-mechanisms-preserve-invariants.md) — the larger more-correct fix is the right call (don't shrink scope to dodge review); the real avoidable failure when rewriting a staleness/cache mechanism is REGRESSION — the old code's INCIDENTAL guarantees (mtime `src.stat()` raising loud on a missing file; STEP-stale forcing all configs so `assert_configs_distinct` ran) leave silently. Characterize old behavior empirically + adversarial self-review (`/code-review`) BEFORE marking ready — Pedro feedback on PR #199
- [Export staleness → digest](export-staleness-digest.md) — the `export` task re-exported ALL ~119 parts every release: (A) `part_stl_stale` required a `<stem>.STEP` that referenced-only parts never get (manifest holds only the assembly now), (B) mtime compare churns on save-cascade/cache-restore. Fixed: staleness now keys on `dodo._stable_artefact_digest` (per-output source digests in `cad/out/stl/export-src.json`); `stamp_render_cache_current()` re-stamps outputs to now after a successful export so the mtime-based downstream guards (`render_offline`, `cut_release` SCENE_JSON) stay truthful — extends [[two-tier-submodule-digest]]
- [Two-tier submodule digest](two-tier-submodule-digest.md) — fixes assembly-only submodule bumps rebuilding all ~100 parts: parts fold the tree MINUS `adapters/solidworks/assembly.py`+`motion.py` (`_submodule_part_digest`), assemblies fold the whole tree; safe on the "parts never CALL assembly/motion" basis — enforced loud by `check:partiso` (`test_part_isolation.py`). MCP-server tree (tools/agents/ui/server) kept IN the part digest (codex #191: can't verify submodule-internal reach from a repo-local scan)
- [Submodule pre-existing ruff debt](submodule-preexisting-ruff-debt.md) — SolidworksMCP-python had 216 ruff errors in tests/ (measured at submodule b72ac309; pointer since advanced — re-check) pre-existing on `personal`, untouched by the flip/CreateMate PRs and not CI-gated; deferred to a standalone hygiene PR to keep the stack scoped clean

- [Single-assembly fast verify](single-assembly-fast-verify.md) — a _common.py edit cascades into a whole-repo doit rebuild; iterate on ONE assembly via standalone build_*.py scripts + `verify.py <stem> --suite soundness` with `HARMONIC_VERIFY_ALLOW_STALE=1` (standalone builds don't update .doit.db, so the freshness guard false-positives)
- [Release manifest filter](release-manifest-filter.md) — cad/out is a persistent cache that accumulates STALE docs (retired/renamed parts, scratch diagnostic assemblies); cut_release.export_neutral now restricts to the build manifest (part_stems + ASSEMBLY_ORDER), logs strays, fails loud on missing. v0.10.0 shipped 2 stale parts before this (PR #93, main ccd05d0)
- [Per-seat part order](per-seat-part-order.md) — cold-build part order is permuted per seat (hostname-seeded via hashlib, NOT PYTHONHASHSEED-salted builtin `hash()`) so two machines split the work through the shared cache instead of both MISSing on the same next part; since the spine→seat-lock switch ([[com-seat-lock]]) it is a best-effort SCHEDULING HINT (correctness = seat lock + re-probe after acquiring the seat), no longer a deadlock risk
- [Checks perf/value audit](checks-perf-value-audit.md) — soundness is ~99% of verify cost (median 989s); gear-ratios is 50% of it and has caught 0 defects ever (demote to release/incremental); health/dof/over-constrained each redundantly ForceRebuild3 the same model (~165s to reclaim); component-count is pure noise; biggest gap is there's NO CI so cheap checks get merged red — extends [[release-perf-incremental]]
- [v0.18.0 perf review](v018-perf-review.md) — 2026-07-09 log-profiled + 3-agent code review, ~80 min/run reclaimable, ranked: non-incremental release export (~1700s, PNG key hashes churning bytes → 9/648 hits), cut/sweep IFeature flag-storm re-regression of c992057 (~800s, zero geometry risk), ground channel cosmetic bank (~750s, reverses #110), build↔verify gate dup, no-op activate rebuilds, O(F²) naming; patterns + batch-select hypotheses verified ALREADY APPLIED
- [Release/build perf — kill COM-spine duplication](release-perf-incremental.md) — profiled v0.9.1 release logs (~153 min cold, all serial on the one STA seat). Two structural wastes fixed: (1) verify `soundness`/`subsystems` were ~95% the SAME battery on all 8 assemblies (≈980 s/run dup; deep model-healthy ran twice) → subsystems trimmed to channel-independence, then later RETIRED ENTIRELY (folded into soundness; `verify:subsystems` no longer exists); (2) `cut_release.export_neutral` re-rendered 486 PNGs EVERY release even with 0 changed parts → PNGs now rendered-but-cached by resolved-geometry fingerprint (`_png_key` = exported STL + source doc + colors.json; miss only ever re-renders). STEP/STL still SaveAs3-exported per doc — a first cut that COPIED them from `cad/out` was a Codex-P1 bug (cad/out is the manifest-driven render cache: only per-mesh STLs + the top STEP, NOT the 81-doc neutral set). **OFFLINE-VALIDATED ONLY — COM paths need one seat run (`doit release -- vX --no-publish`) before next real release**

- [Parametric naming round-trip](parametric-naming-roundtrip.md) — make build scripts emit named SW globals/sketches/features/dims so parts are GUI-editable & round-trip (top-frame = validated reference). 3 COM gotchas: never flag IFeature/currentModel (breaks adapter cut walk → "Parameter not optional"); name_sketch must sync adapter._last_sketch_name; inch docs eval equations in doc units → give globals explicit `mm`

- [Channel-count + amplitude state](channel-amplitude-state.md) — FINAL state on similarity-tuning branch (63dd47e, NOT for main): full 20 active channels + neutral amplitude preset (all a_j=0, matches neutral reference photos). Knobs: machine/channels.yaml active_count=20, machine/amplitude.yaml preset=neutral. Square preset is the documented alternative
- [Oblique views break on-axis occlusion](oblique-views-break-on-axis-occlusion.md) — a part on a column/axis line (gooseneck post @ east x197 z0) is fused with the column in BOTH front+side elevations; a 3/4 view (ch07-img17 az−150) exposes its free end. Vertical scale = top-frame 41mm; cross-check with the part's azimuth-invariant cylinder-width silhouette. Gooseneck tip → machine 880, ~120 below plate (not the ~40 front-view guess)
- [SW assembly mate diagnostics API](sw-assembly-mate-diagnostics-api.md) — how to fetch "What's Wrong" / "View Mate Errors" / over-define info via COM (GetWhatsWrong + byref VARIANT trap, GetErrorCode2, swFeatureError_e 46/47/48); GetConstrainedStatus is gear-mate-blind so probe a mate to read true DOF

- [Tube column OD rederive (M6.11)](tube-column-od-rederive.md) — column OD rederived from ch30 8-views to Ø25.4 (1"), was legacy Ø1.375"; quarter views isolate single columns, base-plate cross-checks scale; full cascade (top-frame bore, clamp bore, clearances); SW-rebuilt + validated 2026-06-19 (402756af)
- [ch30 GT re-anchor](ch30-gt-reanchor.md) — drive plane 104.8, crank (122.8, 144.96) ABOVE the 64T (mesh-azimuth assumption was the bug, not the photos), alignment pinion restored level-inboard, rod 180.83, chain z −155; traps: PEN16_MID is negative, channel frame mixes ±x in one frame, e2062b5 shipped a YAML quote bug; north-region + top-frame GT follow-ups deferred
- [ch14 ROM re-derive](ch14-rom-rederive.md) — cam ecc MEASURED 8.64 (was scaled 3.06, stroke 2.8× understated), lobe +Y at notch-up, arms rest LEVEL (closure co-solved ROD_HOLE_X 127.37 / ROD_C2C 147.67 / RING (54.474, 113.437) + fail-loud assert), p.25 outline = teardrop TEMPLATE cam modelled as circular eccentric (user decision), rod pin LOW (ROD_HOLE_Y 15.30) + tombstone Y-head, LEVER_EYE_Y 1062.52, bushing bar clearance down to 0.63
- [OD 62.2mm gear re-anchor](od-62mm-reanchor.md) — user re-anchored gear OD 103.3->62.2mm (LOW conf eyeball), "fix everything downstream"; DP 30->49.82, crank-drive DP->26.57 (64T==cone T120 match restored), cam/rod/cone all scaled 0.6; forced impossibilities → REMOVE alignment pinion (others didn't block; pinion later RESTORED 2026-07-02, see ch30-gt-reanchor); **SW rebuild DONE+green, all 88 verify gates, render diff vs v0.2.0 shown, LANDED on main fde40f2**; throw scaled to 3.06 (only fallout = 1-fn plate-threading fix); to land it user chose **revert PR#12 "Fix#8"** (sketch-inference suppression), then **Fix#8 RE-LANDED clean in PR#13 (e0bb8d7, HEAD 0ca20cd)** against the 3.06 channel — determinism bug fixed again

- [#8 fixed — inference suppression](headless-render-no-gl.md) is the render note; #8 ROOT-CAUSE FIX: first merged as PR #12 then REVERTED (19f34d4, to land other OD-62.2 work), **re-landed clean in PR #13 (fix-8-inference-interference-2, MERGED 2026-06-19, closes #8)** — source auto-merged with zero conflicts; my stale renders/BOM dropped in favor of main's newer regen (e6feed3), so committed artifacts predate the fix (rebuild to refresh). Fix: SW sketch-inference non-deterministically inflated the summing-lever hex (4549.64 mm³ knife-mount overlap) — fix = `_common.add_line_chain/add_circle` draw with `SketchManager.AddToDB=True` (inference SUPPRESSED) + 2 explicit origin anchors (transgear blank, measuring-stick body; audit_unanchored_sketches.py proves only those 2) + channel banks switched from assembly LocalLinearPattern (face-sense flips at 20 ch, ok at 3) to explicit per-gap placement; exp_inference_{determinism,zoom}.py = the isolated proof. **First full from-empty build GREEN** (harmonic-analyzer.SLDASM, all 4 configs, interference-free); release-diff vs v0.2.0 = 4 changed parts (channel-spring-installed/measuring-stick/rocker-arm/transgear-removable), summing-lever itself byte-identical
- [Headless render: no GL](headless-render-no-gl.md) — no-GPU agent session: REAL fix is Mesa ≤25.0.x self-contained osmesa.dll + VTK 9.4 runtime GL selection (osmesa_win.py, auto-fetched); diff render now works headless, cut_release blocks on diff failure. render_diff_mpl.py is the legacy fallback
- [Per-config STL export race](per-config-stl-export-race.md) — multi-config parts (cone-gear ×20, transgear ×3) shipped a config's neutral STL holding an ADJACENT config: ShowConfiguration2→immediate SaveAs3 with no rebuild captured the prior config (SW regenerates switches LAZILY). v0.5.0 mis-shipped --t24 as 18T, v0.5.1 --t18 as 12T; master CAD always correct. Fix (PR #40, v0.5.2): ForceRebuild3(False)+EditRebuild3() after switch + fail-loud distinct-CRC guard. Validated: ODs 14:20:26, 23 STLs byte-identical ×2 runs; perf +~70s export-only
- [Incremental builds validation](incremental-builds-validation.md) — PR #5 refresh primitive + doit DAG runtime-validated off v0.2.0 bundle (5 stages green); fail-loud = dangling mate→exit 1 + .SLDASM byte-identical; interference gate dominates big-asm timing; **PR #9 (fix-measure-opposite-faces, closes #6+#4) pushed the FULL from-empty build**: 4 infra fixes (bbox gate / UTF-8 stdout / part-builds-export-STL bootstrap / dead summing-lever import) → 85 parts+frame+drive_train+channel GREEN, `output` assembles fully but interference gate correctly fails on reworked summing-lever hex collars (→ #8, design re-integration; rework is in v0.2.0 SOURCE but bundle shipped pre-rework output.SLDASM so v0.2.0 can't rebuild its own output); build env needs pillow+PYTHONUTF8; **re-confirmed on v0.9.0 (post-#71 parametric parts) 2026-06-22** — same brittleness (pen_rod hole-Ø edit → pen Distance2/3 dangle, fail-loud), recovery = rm .SLDASM → FULL re-mate, ID churn cascades up the tree (leaf-part edit needs FULL on every assembly directly mating it), and the post-release "refreshes ALL assemblies once" wrinkle **DIAGNOSED + FIXED 2026-06-22**: NOT part re-stamping (measured 0/73 parts re-stamped) — `save_assembly_in_place` unconditionally force-bumped each `.SLDASM` md5 on every refresh (incl. no-ops), cascading parent invalidation up the tree; fix = gate the bump on an exact-BREP mass-properties fingerprint (`assembly_geometry_digest` + `.massprops.sha` sidecar) so a no-op refresh leaves the `.SLDASM` byte-stable; validated to a clean no-op fixpoint (top skipped); **sharp edge (geometry edit dangles a mate) RESOLVED 2026-06-22 via `IAssemblyDoc.AutoMateRepair` auto-heal in `refresh_assembly`** (~5s in-place re-bind vs ~500s FULL; judge success by What's-Wrong+gates not its PartialSuccess return code; select Mates folder via `FeatureByPositionReverse`; a heal forces the save so re-bound PIDs persist; gates remain the mis-heal safety net) — proven on channel/pivot-shaft (20 dangling J1 concentrics → healed, gates green)
- [mm normalization + render-ready release](mm-normalization-render-bundle.md) — render is unit-agnostic (mesh==transform unit is all that matters); old cache mixed m/mm so the mm release stl/ couldn't pair → normalized ALL to mm (Phase 1 commit 2840e4b: export_models STL+scene ×1000, stl_bbox_mm drops ×1000, NEEDS export_models --force regen); cut_release now ships boxes/ scene graph + 23 per-config STLs (cone-gear 20 + transgear-removable 3, the ONLY multi-config parts) so render_offline renders from a downloaded release (Phase 2 commit c5544f7); eccentric-cam REMOVED 2026-06-21 (integral to cylinder gear) + pinion-drum dead; part count now ~94; NOT YET SW-VALIDATED
- [Comparison top-5 photo fixes](comparison-top5-photo-fixes.md) — 2026-07-08 gallery vision pass (PR #209): casting_green was teal (re-sample palette claims with PIL medians), coplanar decal faces z-fight in offline renders (0.25 air gap; interference gates can't catch it), summing lever/pen v-block green + magnifying wheel black via apply_color, wheel "8 spokes" was a render miscount (probe the STL first), _common.py palette edits rebuild the whole fleet
- [Comparison pose fitting is vision-first](comparison-pose-vision-first.md) — iterate az/el/roll/target/zoom by reading the blend overlay; run tune_align IoU / RMS optimizers only for the final 2D polish once the 3D pose is within mm (optimizers latch onto wrong local optima while orientation is off)
- [Comparison camera refinement](comparison-camera-refinement.md) — 8-view cameras were a turntable approx; fit az/el/roll by silhouette-IoU vs photos (el now −7..+10°, commit 9f842ec), re-fit align/pan-zoom (b15ed24); regen colored gallery WITHOUT SolidWorks via release STEP→cascadio glb (Z-up, rotate −90° X) + colors from build-script MATERIAL/apply_color cascade (commit 1e541fa); scores dropped on all 8; headless Linux Workbench needs libEGL+llvmpipe (slow), GPU fast
- [Summing lever true geometry](summing-lever-true-geometry.md) — real lever has NO bore; hex protrusions at the pivot form the knife edge (first-class lever in suspension); reverting build_summing_lever.py to the .cs shape + adding hex + moving spring holes to channel registration + full output-assembly re-integration; .cs summation-anchor eye ≈ counter-spring hook (machine 91 vs 90.5); hex size LOW conf (tune vs ch30); plan serialized-wobbling-lerdorf.md. **2026-06-25:** 20 channel springs join the plate via a NEW separate `spring-hook` (MHA-090, open-J, shank-up in Ø4.5 bore) NOT a plate-threading eye — eye moved ABOVE plate (PLATE_EYE_Y 996.54, body 62.61); traps logged: local+X→world−X frame flip; eye-RING bottom (not centre) must clear the plate; and the big one — `place_components_batch` mirrors ALL grounded specs via `MIRROR_PLANE.get(part,"x")` default, so a CHIRAL grounded part with no entry gets X-flipped (spring-hook needs `("z",0.0)` like the spring/boss-hook); interference=0 ≠ engaged, RENDER to check fit (diag_hook_engage.py)
- [Pause between phases](feedback-pause-between-phases.md) — on approved multi-phase plans, finish ONE phase, report, STOP for sign-off before the next; don't chain phases
- [output split + loose-part collision](output-split-and-loose-part-collision.md) — output.SLDASM split into 4 FLAT subs (summing/magnifier/pen/paper-drive); Phase 1 DONE+green, MERGED as PR #41 (2026-06-20); Phase 2 semantic/datum-locate mates began 2026-06-27; GOTCHA: a loose top-level part whose part-type is ALSO nested in a sub collides on leaf name in fix_component → house it inside that sub as a flat sibling (spare T18 → paper-drive)
- [Assembly single-config](assembly-single-config.md) — 2026-06-19 user directive: removed ALL assembly non-Default configs (engagement enum cone_disengaged/operating on drive-train + top); deleted build_engagement_configs/build_operating_config/build_top_engagement_configs/reset_pose, emptied _buildgraph POST_ASSEMBLY, ripped the verify engagement suite (agate/_open_isolated/TOP_OWNER); drive-train reverts to native fix_component grounding; PART-level multi-configs (cone-gear teeth, transgear) KEPT; .SLDASM gitignored so next doit build regenerates single-Default (NOT yet SW-rebuilt)
- [Professionalize plan status](professionalize-plan-status.md) — HISTORICAL ~2026-06-16 snapshot (suites since RENAMED 2026-06-20: static→soundness, truth→math, isolation→subsystems[later retired], motion→kinematics, engagement→removed; `build_all.py` removed 2026-06-18 — gate counts have drifted, check git log): at that time **PART F COMPLETE** (A/B/C/D/E + F1 isolation 27/27 + F2 truth + F3 amplitude-bars + F4 engagement 8/8→6/6 (pinion_engaged retired) + F5 motion 3/3, all live-validated); plan step 9 met; NEVER run 2 SW scripts at once (STA deadlock)
- [Part D custom properties](part-d-custom-properties.md) — DONE+validated-live; registry NOW per-part under cad/config/parts/ (the single parts.yaml + the 77/85 counts + config 14/14 gate + "remaining = Part F" are a 2026-06-15 snapshot — drifted to ≈87→79 parts / ≈13 gates, Part F long done); MECHANISM unchanged: SW custom props via raw-COM CustomPropertyManager.Add3 (adapter has no writer), Generator=git sha no wall-clock, tolerance audit in verify.py --suite config, CSV gitignored
- [Pen equation driver (F5)](pen-equation-driver.md) — DE-RISKED 2026-06-16; SW equation mgr rejects a 20-term sum in ONE expression (length ceiling), fix = chained partial-sum globals S1..S20 (Sk = "S{k-1}"+term), PenY="Magnify"*"S20"; cascade re-eval through 20-deep chain works, matches truth_model.pen_y to FP precision (7.95e-13mm); **DONE+INTEGRATED 2026-06-16** (pen_driver.py now installed by build_pen_assembly.py — was output.SLDASM before the 2026-06-20 output split; verify.py --suite kinematics 3/3 — suite renamed from motion; top re-validated, throwaway probes deleted 5e68332)
- [Verify SW API with research](verify-sw-api-with-research.md) — when an API claim stops making sense, spin off a research agent (forums/SO/CADBooster/CodeStack) before declaring a dead end; GetCorresponding vs GetCorrespondingEntity was the proof
- [SW assembly mate diagnostics API](sw-assembly-mate-diagnostics-api.md) — debugging over-define/unsolved mates: GetWhatsWrong (byref VARIANT trap) + GetErrorCode2 + swFeatureError_e codes (46=over-define warn, 47=flip); GetConstrainedStatus is BLIND to gear coupling (probe-mate to tell 1-DOF from closed loop); distance-mate far-side over-define → _mate flip-recovery on unsolved flip=False (crank hang-down fix a45a5ea)
- [Parametric springs](parametric-springs.md) — a spring whose endpoints move must be LENGTH-PARAMETRIC, not fixed-length; fix = grounded-at-measured-length per channel. At the NEUTRAL preset (all a_j=0, current default) the assembly instantiates ONE canonical channel-spring-installed body ×20 with ZERO stretch variants (PR #61); stretch{NN} bodies only appear at nonzero amplitudes. DONE+VALIDATED: channel + top interference NONE. Also: SW grounding/structural mates must be added with REST config active (else they solve vs a non-Default child → Default under-defines)

- [Amplification wires](amplification-wires.md) — both wires MODELED (Ø0.8 rods); WIRE 1 is a LIVE book-true chain in magnifier (build_lock free, 3 DOF: lever KNIFE-rock about the summing ridge (video 2/4+4/4, collar = loose guide) + articulated wire (hook ball joint + axis-axis 10.65 hub tangency) + WireYokePoint↔YokePlane yoke → linear 17.2:1, gated by verify chain:magnifier:*); DEPTH RE-ANCHORED to the ch30 p.4 plumb line (lever z −128.3, the ONLY window: ring rail/column/rim-duck); gear lever(X)↔wheel(Z) REJECTED (skew axes); WIRE 2 still F5-equation-pinned (yoke would over-define; phase 2 open)
- [Flexible subassemblies](flexible-subassemblies.md) — Phase E gate PASSES; recipe = _select_component + CompConfigProperties5(2,1,...); bugs were bare-None callout + FullyResolved=2-not-3
- [Fix relations are a last resort](fix-relations-last-resort.md) — replace ALL fix anchoring with semantic relations + driving dims; surviving fix needs inline justification
- [Harmonic analyzer project decisions](harmonic-analyzer-project.md) — M6.5 closed the ORIGINAL 7-milestone plan 2026-06-11 but development CONTINUED (M6.11+ and later PRs through 2026-07-04; the milestone entries are historical records, check git log for current status); orientation (current): crank at −X (x≈−118), front = −Z paper/output side; ch30 az = 180+45·(page−2), all true photos, derive azimuth from content/position arithmetic; STANDARD-VIEW REMAP (2026-06-16): SW Front shows the machine front via `_common.remap_front_to_machine_front`→`UpdateStandardViews` (geometry NOT moved, .SLDASM opens-on-Front, euler az0=+Z still valid); offline Blender pipeline merged into main (comparisons/tools/, export_models.py; cad/out stl/step/boxes gitignored); M6.7 TRUE-CONE drive train (incline 21.1°, X_PITCH 4.490); PHOTO-TUNING (broad comparison set OUTDATED — only ch30 8-views trustworthy): q90 JPEG, submodule two-step commits; ch30 8-view tune DONE 2026-06-12 (palette, platen-paper + drive-chain parts, shaded-no-edges renders; deferred: lever-fan droop, black bg, marker tilt); ch23 transgear rework DONE 2026-06-12 (gears-too-big verdict: model correct, chain wheels were the fix); ch25 REWORKED FRONT-CENTRE 2026-06-12 (p002 silver cluster = engage lever + turning handle CONFIRMED; pinion script (−2.18, 70.5), pivot +27.85, lift +42.85 spacing 15, strap c2c 31 lean 75.6°, drum 143.2, j19 face 90% — knob-post cap, Appx C #10/#11; check BOTH cone posts' plan footprints, per-disc beats bbox); M6.9 ROCKER-SUPPORT PORTAL FRAME DONE 2026-06-12 (ch30 p008 proves windowed frame ~184×127, legacy reading reinstated; a-frame re-authored machine-handed MIRROR_PLANE "x0" — one-sided geometry invalidates "z" Ry180 stand-in; plate 18.5 pinned by stick/handle; arbor 196, pedestal z −90.5; deferred: round cone-pivot-post); M6.10 FASTENERS DONE 2026-06-12 (5 fastener parts + 7 hosts holed, output 60 comps, frame +2 lag screws, BOM 101; "x0" placement = MIRRORED ORIGIN + locals AS-AUTHORED — author new features against the MACHINE origin; interference volume matching an analytic formula = the diagnosis; pinch screws backed out; tapered pin / fixture screw / clevis / nameplate still omitted); book photo annotations authoritative; every part/assembly needs a Python reproduction script; MCP-first workflow
- [M6.3 channel layout](channel-layout-m63.md) — rocker seesaw pivot (−72.9, 253.8); bars go UP to top levers; M6.5: ONE support frustum (north, +boss for arbor), fulcrum-shaft 182 split off; photo calibrations front/back/side
- [Build GDI session accumulation](build-gdi-session-accumulation.md) — TWO SW session-age failure modes. (1) GDI exhaustion is RARE (user 2026-06-17): do NOT pre-emptively restart/raise-quota on a high idle baseline; only act on an ACTUAL rm_gdi modal/hang, tell user if recurring; GDI accrues across scripts & isn't freed; instances cheap (~20/comp), distinct parts ~140-180; measure via GetGuiResources P/Invoke. (2) Progressive per-op SLOWDOWN with GDI fine (2026-07-06): identical gear mates 4.7s→35.5s (7.5×) — culprit was a RUNAWAY TextInputHost (66% of a core, 9.3 CPU-h) starving SW's STA message pump; an SW restart did NOT help, killing TextInputHost fixed it instantly (respawns clean). Diagnose: diff SAME-label mate spans vs the release `-logs.zip` (task totals mislead — cache upload), then per-process CPU RATE (5s Get-Process delta)
- [SW zombie-doc file lock](sw-zombie-doc-lock.md) — a doc can survive CloseDoc/CloseAllDocuments(True)=True while OS-locking its .SLDPRT; only SaveAs to that path fails ("Failed to save as", other parts build fine); diagnose with mv-locktest + GetFirstDocument walk, remedy kill + 3DX-shortcut relaunch
- [SW Document Recovery dialog](sw-recovery-dialog.md) — after force-killing SW + 3DX relaunch, a "Welcome → Document Recovery" modal lists the killed session's auto-saves (junk Assem8) with Open All/Delete All; detect it via process-up + GDI-low + MainWindowTitle='' ; clear with UIA Invoke (EnumWindows title-match fails; BM_CLICK no-ops) → Delete All → Yes (never Open All; real parts are in cad/out) BEFORE running the build
- [SolidWorks 3DEXPERIENCE launch](solidworks-3dx-launch.md) — never start sldworks.exe directly (Makers licence rejects it); COM is ONLY for attaching to an already-shortcut-launched SW, NEVER for starting it (COM-relaunch yields an instance that reports "not started via 3DEXPERIENCE" = unlicensed); recovery = clear crash dialog then launch the Platform desktop shortcut (.lnk), THEN connect()
- [M6.4 output layout](output-layout-m64.md) — HISTORICAL M6.4 (2026-06-10) placement snapshot, NOT current (build_output_assembly.py since split; many coords MOVED, e.g. STICK_POS now ≈(−212,53.8,−100)): crossbar 1010→202 long, spring 1052.1, rack 15.5p phase, pen frame flat-on-X; M6.5: a-frame clevis grips south pivot ball, pinion bar −58..+178, size trims
- [knife-stay removed](knife-stay-removed.md) — knife-stay part deleted entirely (never existed in real device); full removal re-keyed all 8 assemblies → next `doit` does a ~40-60min full-machine rebuild (identical geometry except summing); COM rebuild deferred
- [Phase F motion-study](phase-f-motion-study.md) — scope: incremental kinematic gate + springs + wires, architecture proven 2026-06-13/14 (Axis3 lobe-axis cam couplings 612s→1.02s, depth-2-safe GetCorresponding/PR#64, point-on-axis cams via arc-center RefPoints, WIRE1/WIRE2 couplings, CH/CT spring constants G_STEEL=79.3GPa); pipeline gates PASS
- [Motion study pipeline](motion-study-pipeline.md) — Phase F recipe PROVEN on drive-train (cam swept 270°); Basic Motion (physical_simulation) NOT MotionAnalysis (unlicensed Makers seat); create name="" → suppress driver mate → rotary motor → calculate → set_motion_time+component_transform sampling; FULL device = FLEXIBLE subs (NOT dissolve — dissolve deletes driver mates, proven); suppress sub-internal drivers by retargeting adapter.currentModel at comp.GetModelDoc2() then restore (never save sub)
- [Motion video export](motion-avi-export.md) — SaveToAVI(.avi) fails headlessly (codec dialog, no API; recorder won't capture Save Animation); fix = OutputType MP4/MKV/FLV writes native H.264 with no dialog; method renamed export_motion_video; poll file size (async write), Stop()+restore window+screen renderer; PR-M3 branch motion-video-export → personal
- [OTel trace local viewing](otel-trace-local-viewing.md) — verify PR #76 telemetry locally. **Docker IMPOSSIBLE here** (Azure AMD `NV6ads_A10_v5`, no nested virt). Preferred: **Aspire dashboard run NATIVELY** (no Docker) via `.tools/start-aspire.ps1` — UI :18888, OTLP/HTTP :18890 (== spine default → zero-env), logs+traces unified. Fallback: Jaeger (`.tools/jaeger`, :4318, traces-only) with `OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318`. Bounded traced build via `dodo._run(...)` under `run_pipeline_span` bypasses the COM-spine drag (else `_common.py` change rebuilds the whole model). Gotcha: `SimpleSpanProcessor` flushes parents on span END → a killed build shows orphan children, not a real gap
- [Release refresh modal-dialog block](release-refresh-modal-dialog-block.md) — `doit release`/refresh failed at first COM step (`Failed to open model: frame.SLDASM`) while full build was green minutes before; root cause = a leftover modal **"Open" file dialog** jamming the STA COM seat → `OpenDoc6` returns null with **error=0/warn=0/docCount=0** (silent). Build creates assemblies FULL (never reopens); release REFRESHES (reopens existing `.SLDASM`) — only the reopen calls OpenDoc6-on-existing, so only it breaks. Diagnose: `GetDocumentDependencies2(asm,True,False,False)` rules out missing refs; EnumWindows→SLDWORKS pid finds the `WindowsForms…"Open"` dialog; `SendMessage(hwnd,WM_CLOSE)` dismisses → re-run clean. v0.9.1 published, 0 geometry changes vs v0.9.0
- [rocker-support window faces X](rocker-support-window-faces-x.md) — rocker-arm-support is authored window-normal=local-Z; placing at IDENTITY in frame.SLDASM points it ±Z (edge-on from +X, WRONG vs book ch30 p008). Fix = turn +90° about Y (ROT_Y_POS90, grounded place_component) → window faces machine ±X; SUPPORT_Z=21.6 (north face flush w/ ball-mount north edge z110.5 — most-centred that still seats the mount; don't push to z=0). Also build the part green (Gray Cast Iron + CASTING_GREEN). Shaft runs along Z
- [SolidWorks modeling pitfalls](solidworks-modeling-pitfalls.md) — on-axis revolve breaks later booleans (use extruded circles); FeatureCut4 27 params, cuts default OPPOSITE the sketch normal (blind face-cuts need NO reverse; a reversed air-cut can fall back to a flipped-side cut); reference-plane sketches enumerate the plane's dim first (owner-filtered in _common); helix tessellation slack; gear seed gap at +γ/2 (tooth at bottom dead centre); inclined-cylinder clearance = r/cos(i) ellipse + r·sin(i) z-reach; cone-on-drum incline from DRUM pitch (sin i = step/drum-pitch, centres r·sin i north, oblique slacks checker-arbitrated); interference checker fails 0.00 mm³ slivers — design 0.25 margins; inference snaps near-origin sketch points (volume-gate every dim); unabsorbed sketches render SHOWN everywhere (Visible int 2); material .p2m appearance beats doc MPV — colour at body scope too; shaded-with-edges paints fluted/fine geometry black — comparisons render ViewDisplayShaded; mirror_placement needs STL bbox at insert — new symmetric parts get explicit MIRROR_PLANE ("x", 0.0) entries
- [stdbuf LD_PRELOAD breaks MSYS git push](stdbuf-ld-preload-breaks-msys-git.md) — never wrap a git-pushing pipeline in stdbuf on this machine: its Windows-path LD_PRELOAD kills credential-helper sh.exe ("shared libraries: C:"), push goes anonymous; PYTHONUNBUFFERED=1 suffices. Cost two 43-min release runs
- [Cache partial-mix dangle remedy](cache-partial-mix-dangle-remedy.md) — local part rebuild + foreign cached assembly = dangling mates [48] at deep-health; deleting the .SLDASM alone RE-RESTORES the broken artifact (key still hits) — close SW docs, delete asm+sidecars, rebuild with HARMONIC_REMOTE_CACHE_MODE=off, also delete the half-built downstream asm, then normal rw run; diagnose with doit cache_status (hit/miss mix + DRIFT flags)
- [Auto-repair opt-in decision](auto-repair-opt-in-decision.md) — cache-restored What's Wrong [48] dangles remain loud by default; `verify.py <assembly> --suite soundness --auto-repair` is the explicit retry. It repairs only code 48, re-reads clean, runs the full soundness battery, and saves locally only after every gate passes; never republishes under the foreign cache key.
- [paper-drive park-closure gate](paper-drive-park-closure-gate.md) — RESOLVED BY REMOVAL 2026-07-09 (park machinery deleted; close issue #205). Durable SW fact: gear/rack-pinion/belt-chain couplings are MOTION-only and never reduce the fully-defined count — a 0-DOF assert over a gear-coupled train is unsatisfiable by construction; list coupled families as allowed-under-constrained instead
- [Stacked-PR merge order](stacked-pr-merge-order.md) — `gh pr merge --delete-branch` on a PR that is the BASE of a stacked child CLOSES the child instead of retargeting it (bit #148 when #152 merged and pr2 was deleted). Safe order: retarget the child first (`gh pr edit <child> --base main`), THEN merge + delete the parent branch
- [Submodule pointer drift](submodule-pointer-drift.md) — '+' in git submodule status = checkout != recorded pointer; pydantic silently drops newer adapter kwargs (is_cut boss-not-cut bug)
- [Never say final](never-final.md) — decisions stay revisable; say 'current' or 'as decided (date)', name what it supersedes
- [Park-driver singularities](park-driver-singularities.md) — a park mate can author "OK" yet pin NOTHING (angle-to-plane cone apex, or gradient ⊥ the DOF — axisymmetric parts are point-constraint-blind to spin) or fail IN PLACE (far-side distance created in hard error 47 with zero motion, invisible to motion-based flip recovery; `_mate` now checks GetErrorCode2). Debug with diagnostics/probe_magnifier_closure.py; status 6 = swInvalidSolution
- [Paper-drive real train](paper-drive-real-train.md) — PR #196 final six-gear topology; Appendix C #8 closed; <63T pinions need centre extension (gap floor at base circle) and 12T needs a Ø5 seat
- [Paper-drive rework evidence](paper-drive-rework.md) — the full E1-E8 dossier + part-change table behind PR #196 (moved from docs/); anchors, z-stacks, mate chain, photo cross-checks
- [Codex review diminishing returns](codex-review-diminishing-returns.md) — judge Codex rounds by finding quality, not count: keep fixing real bugs, decline only over-pinning
- [Mirror retirement sweep](mirror-retirement-sweep.md) — #151 golden-pose gate, rulebook, accepted deltas
