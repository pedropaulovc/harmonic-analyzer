counter-spring:
  number: MHA-019
  material: Alloy Steel
  tolerance_class: machined_block
  fit_class: spring_eye_threading
  process: coiled wire
  dimensions:
    chapter: Chapter 19 — Counter Spring (pp. 44–45)
    content:
    - notes: |2

        No annotated or stated numeric dimensions. No legacy part. M6.4 REVISION: the
        M2 "~300 × Ø22, wire ~2.5" read came from the cut-off p1 front page (the
        spring exits the page top). Recalibrated against the ch. 19 full-machine photo
        (gooseneck scale 0.515 px/mm) and the p3 90° page.
    - table:
      - dim: Coil body length
        value: 315 mm
        (in): '12.4'
        source: ch. 19 full-machine photo, gooseneck-scaled (supersedes the ~300 M2 read)
        method: scaled
        confidence: low
      - dim: Coil OD
        value: 12.5 mm
        (in): '0.49'
        source: vs the Ø16 gooseneck tube in the same frame (supersedes ~22)
        method: scaled
        confidence: low
      - dim: Wire dia
        value: 1.8 mm
        (in): '0.071'
        source: close-wound dark coil, no light through (supersedes ~2.5)
        method: scaled
        confidence: low
      - dim: Coil count
        value: '165'
        (in): —
        source: 'close-wound: pitch 1.91 leaves a 0.11 sweep-merge gap'
        method: derived
        confidence: low
      - dim: Bottom lead
        value: straight 40 mm drop, coil bottom → ring that hangs on the boss hook (ring centre machine y 1012, rod top 1015)
        (in): —
        source: 'M6.4 hang solve (`build_summing_assembly.py`: ring inner top 1016.45 vs rod top 1016.5)'
        method: derived
        confidence: low
      - dim: Adjustment post
        value: square-head screw on adjustable post
        (in): —
        source: text pp. 44–45
        method: stated
        confidence: high (feature)
      - dim: Gooseneck
        value: 'Ø16 chrome tube: vertical leg machine x 197 (east column line), y 880..1390 — the post drops ≈120 below the rail underside (999.7) and ends in a free tip, passing through a Ø17 clearance bore in the east rail (`build_top_frame`) and gripped by the clamp above; 180° bend R 51; tip leg x 95 — plumb above the boss hook — ending y 1378; lug + Ø4 X-pin at y 1373 carries the spring''s top loop'
        (in): —
        source: 'tip drop: ch. 7 back-left ¾ view (page001_img17) — free tip clear of the columns at image y 491, machine 880 by frame-height scale (41 mm/26 px), cross-checked by the Ø16 silhouette (10 px). Leg-top/bend: 0.515 px/mm + p3 90° page; tip "slotted screw" modeled as lug + pin'
        method: scaled + derived
        confidence: leg-top med, drop med
      - dim: Gooseneck clamp
        value: green cast block 30 × 29 × 24 at machine (197, 1040.7, 0) on the east rail end; Ø16.5 vertical bore; square-head pinch screw (10 × 10 × 6 head, Ø5 shank stopped at the bore wall)
        (in): —
        source: 'p.45: "a square-head screw pinches the post in its socket"'
        method: scaled
        confidence: low
    - notes: |2

        Notes: single large spring balancing the combined pull of the 20 channel
        springs. Hang chain (machine y): boss hook rod 1015 → bottom ring 1012 →
        coil 1052..1367 → top loop 1370.6 → gooseneck pin 1373. Tension is set by
        sliding the tube in the clamp; the post passes through the east rail's Ø17
        clearance bore and drops ~120 below the plate underside, ending in a free
        tip at machine y 880 (below the rail bottom 999.7) — the support extends
        well below the top plate (ch. 7 page001_img17 back-left ¾ view).
