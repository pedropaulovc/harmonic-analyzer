# Learned flip-polarity seeds for the channel assembly (issue: per-assembly
# flip seeds). Rebuild with these empty + read the `_mate` `learn: add sig`
# warns to re-derive. Seeds are an optimisation; a miss self-heals via the
# readback guard.
seeds:
  - "fulcrum shaft datum x"
  - "fulcrum shaft datum y"
  - "lever axial seat"
  - "lever bushing axial z"
  - "pivot ball mount datum x"
  - "pivot ball mount datum y"
  - "pivot ball mount datum z"
  - "pivot bushing axial z"
  - "spring hook datum y @npn"
