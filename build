#!/bin/sh
# Farm entry point: doit schedules locally; every cache-missing part/assembly/
# drawing builds on the SolidWorks farm (see build.py --executor farm).
cd "$(dirname "$0")" && exec uv run python build.py --executor farm "$@"
